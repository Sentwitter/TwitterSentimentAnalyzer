h2 class="text-center" Présentation de la recherche : #{@tweet_search.full_text}

= will_paginate @tweets

div
  table class="tweets table table-striped table-bordered"
   thead
     tr class="text-center"
       td 
       	b id
       td 
       	b twittos
       td 
       	b contenu
       td 
       	b annotation
       td
        b annotation à la main
     - @tweets.each do |tweet|
      == render 'tweet', tweet: tweet

= will_paginate @tweets